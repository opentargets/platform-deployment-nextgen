{{ define "service.webapp" }}
{{- $prefix := include "prefix-product" . }}
{{- $namespace := include "namespace" . }}
{{- $labels := include "labels" . }}
---
# Service for {{ $namespace }} WebApp.
apiVersion: v1
kind: Service
metadata:
  name: {{ $prefix }}-webapp
  namespace: {{ $namespace }}
  labels:
    app: webapp
    {{- $labels | nindent 4 }}
  {{- if .Values.gcp.enabled }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/backend-config: '{"default": "{{ $prefix }}-backendconfig-cdn"}'
  {{- end }}
spec:
  selector:
    app: webapp
  ports:
    - port: {{ include "ports.http" . }}
      targetPort: {{ include "ports.http" . }}
      protocol: TCP
      name: http
  type: NodePort
{{- end }}


{{ define "service.api" }}
{{- $prefix := include "prefix-product" . }}
{{- $namespace := include "namespace" . }}
{{- $labels := include "labels" . }}
---
# Service for {{ $namespace }} API.
apiVersion: v1
kind: Service
metadata:
  name: {{ $prefix }}-api
  namespace: {{ $namespace }}
  labels:
    app: api
    {{- $labels | nindent 4 }}
  annotations:
    {{- if .Values.gcp.enabled }}
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/backend-config: '{"default": "{{ $prefix }}-backendconfig-no-cdn"}'
    {{- end }}
    prometheus.io/scrape: {{ if eq .Values.product "platform" }}"true"{{ else }}"false"{{ end }}
spec:
  selector:
    app: api
  ports:
    - port: {{ include "ports.http" . }}
      targetPort: {{ include "ports.apiproxy" . }}
      protocol: TCP
      name: api
  type: NodePort
{{- end }}


{{ define "service.aiapi" }}
{{- if .Values.aiapi.enabled }}
{{- $prefix := include "prefix-product" . }}
{{- $namespace := include "namespace" . }}
{{- $labels := include "labels" . }}
---
# Service for {{ $namespace }} AI API.
apiVersion: v1
kind: Service
metadata:
  name: {{ $prefix }}-aiapi
  namespace: {{ $namespace }}
  labels:
    app: aiapi
    {{- $labels | nindent 4 }}
  {{- if .Values.gcp.enabled }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/backend-config: '{"default": "{{ $prefix }}-backendconfig-no-cdn"}'
  {{- end }}
spec:
  selector:
    app: aiapi
  ports:
    - port: {{ include "ports.http" . }}
      targetPort: {{ include "ports.http" . }}
      protocol: TCP
      name: aiapi
  type: NodePort
{{- end }}
{{- end }}


{{ define "service.clickhouse" }}
{{- $prefix := include "prefix-product" . }}
{{- $namespace := include "namespace" . }}
{{- $labels := include "labels" . }}
---
# Service for {{ $namespace }} ClickHouse.
apiVersion: v1
kind: Service
metadata:
  name: {{ $prefix }}-clickhouse
  namespace: {{ $namespace }}
  labels:
    app: clickhouse
    {{- $labels | nindent 4 }}
  annotations:
    prometheus.io/scrape: {{ if eq .Values.product "platform" }}"true"{{ else }}"false"{{ end }}
spec:
  type: ClusterIP
  selector:
    app: clickhouse
    color: {{ .Values.color }}
  ports:
    - port: {{ include "ports.clickhouse" . }}
      targetPort: {{ include "ports.clickhouse" . }}
      name: http
    - port: {{ include "ports.clickhouse.native" . }}
      targetPort: {{ include "ports.clickhouse.native" . }}
      name: native
    - port: {{ include "ports.clickhouse.metrics" . }}
      targetPort: {{ include "ports.clickhouse.metrics" . }}
      name: metrics
{{- end }}


{{ define "service.opensearch" }}
{{- $prefix := include "prefix-product" . }}
{{- $namespace := include "namespace" . }}
{{- $labels := include "labels" . }}
---
# Service for {{ $namespace }} OpenSearch.
apiVersion: v1
kind: Service
metadata:
  name: {{ $prefix }}-opensearch
  namespace: {{ $namespace }}
  labels:
    app: opensearch
    {{- $labels | nindent 4 }}
  annotations:
    prometheus.io/scrape: {{ if eq .Values.product "platform" }}"true"{{ else }}"false"{{ end }}
spec:
  type: ClusterIP
  selector:
    app: opensearch
    color: {{ .Values.color }}
  ports:
    - port: {{ include "ports.opensearch" . }}
      targetPort: {{ include "ports.opensearch" . }}
      name: http
    - port: {{ include "ports.opensearch.perf" . }}
      targetPort: {{ include "ports.opensearch.perf" . }}
      name: perfanalyzer
{{- end }}
