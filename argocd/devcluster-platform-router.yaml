apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: devcluster-platform-router
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/opentargets/platform-deployment-nextgen.git
    targetRevision: argocd-rebase-observability
    path: helm/router
    helm:
      valueFiles:
        - /profiles/devcluster/platform/router.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: devcluster-platform
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
